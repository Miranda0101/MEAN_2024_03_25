import { MyFirstDirective } from './my-first.directive';

describe('MyFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFirstDirective();
    expect(directive).toBeTruthy();
  });
});
